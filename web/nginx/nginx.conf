events {}

http {

  server {
    listen 80;

    location = /stub_status {
      stub_status;
      allow all;
    }

    location / {
      return 200 'nginx ok';
      add_header Content-Type text/plain;
    }
  }
}

